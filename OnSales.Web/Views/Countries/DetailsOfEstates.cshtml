@model OnSales.Common.Entities.Estate


<link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
<br />
<br />

<div style="margin-left:10px;" class="chip">
    <img src="https://a.espncdn.com/i/teamlogos/countries/500/arg.png" alt="">
    @Html.DisplayFor(model => model.Name)
</div>

<div style="margin-left:10px;" class="chip">
    NO° de Municipios <span class="mate" style="font-size: larger; color:#64B5F6;">{@Html.DisplayFor(model => model.NumberMunicipalities)}</span>
</div>

<br />
<div class="row">
    <div class="col s1">
        <a id="button_add" asp-action="Details" asp-route-id="@Model.IdCountry" style="color: #64B5F6;" class="material-icons">reply_all</a>
    </div>

    <a id="button_add" asp-action="AddMunicipality" asp-route-id="@Model.Id" style="color: #64B5F6;" class="material-icons">add_box</a>

</div>

<table id="example" class="mdl-data-table" style="width:100%;position:relative;">
    <thead>
        <tr>
            <th>Municipios</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>

        @foreach (var item in Model.Municipalities)
        {
            <tr id="row_@item.Id">
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>

                <td style="display:flex;justify-content:center;">
                    <a id="button_edit" asp-action="EditMunicipality" asp-route-id="@item.Id" style="color: #64B5F6;" class="material-icons">edit</a>
                    <a id="delete-button" style="color: #d32f2f;" class="material-icons x" onclick="DeleteMunicipality('@item.Name.ToString()', '@item.Name.ToString()',@item.Id);">delete</a>
                </td>
            </tr>

        }

    </tbody>
</table>

<partial name="_ViewPageDelete" />
@section scripts{
    <script src="~/js/jsIndexFunctions.js" type="text/javascript"></script>
    <script src="~/js/jsDeleteEstates.js" type="text/javascript"></script>;}